# ==================================
# MentorMatch Environment Configuration
# ==================================
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to version control!

# ==================================
# Database Configuration
# ==================================
# PostgreSQL connection details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mentormatch
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Optional: Database schema (defaults to 'public' if not set)
# DB_SCHEMA=public
# For development with test schema: DB_SCHEMA=test

# ==================================
# JWT Authentication
# ==================================
# Secret key for signing JWT tokens (use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ==================================
# SendGrid Email Service
# ==================================
# API key from SendGrid dashboard (https://app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Verified sender email address
FROM_EMAIL=noreply@yourdomain.com

# Optional: SendGrid region (for EU data residency)
# SENDGRID_REGION=eu

# ==================================
# Google APIs (OAuth, Calendar, Gmail)
# ==================================
# Get credentials from: https://console.cloud.google.com/apis/credentials

# OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# OAuth 2.0 Client Secret
GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth Redirect URI (must match Google Console configuration)
GOOGLE_REDIRECT_URI=http://localhost:5000/api/google_auth/callback

# Optional: Google Project ID
# GOOGLE_PROJECT_ID=your-project-id

# ==================================
# Server Configuration
# ==================================
# Port for the backend server
PORT=5000

# Node environment (development, production, test)
NODE_ENV=production

# ==================================
# Optional: CORS Configuration
# ==================================
# Comma-separated list of allowed origins for CORS
# CORS_ORIGIN=http://localhost:3000,https://yourdomain.com

# ==================================
# NOTES FOR DEPLOYMENT
# ==================================
# 
# 1. Database Setup:
#    - Ensure PostgreSQL is running and accessible
#    - Run the schema: psql -U user -d mentormatch -f DataBase/Mentormatch.sql
#    - Update DB_HOST to your database server (e.g., postgres container name or cloud DB endpoint)
#
# 2. Google OAuth Setup:
#    - Create project at: https://console.cloud.google.com
#    - Enable APIs: Google Calendar API, Gmail API
#    - Create OAuth 2.0 credentials
#    - Add authorized redirect URIs
#    - First-time setup requires completing OAuth flow in browser
#
# 3. SendGrid Setup:
#    - Sign up at: https://signup.sendgrid.com
#    - Verify sender email address
#    - Create API key with Mail Send permissions
#
# 4. Security:
#    - Use strong, unique values for all secrets
#    - Never expose .env file publicly
#    - Rotate secrets regularly
#    - Use environment-specific .env files (.env.production, .env.staging)
#
# 5. Docker Deployment:
#    - Pass environment variables using --env-file flag:
#      docker run --env-file .env mentormatch:latest
#    - Or use docker-compose.yml with env_file directive
